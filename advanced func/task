1. Создать solver методом дихотомии для решения уравнений
вида f(x)=0 на отрезке [a;b], где y=f(x) — непрерывная
функция, значения f(a) и f(b) — разного знака, с заданной
точностью (по оси Oy)) eps. Применить к решению уравнения
вида 2^x = x^2 для x<0.

2. Создать функцию для вычисления arctan x по формуле
разложения в ряд Тейлора (см.рис) до заданного n.
Проверить для x=0.4, n = 50, 100, 150, 200 :)
Создать функцию для вычисления с заданной точностью
eps.

2*. Рассмотреть то же самое, но не для чисел Double,
а для чисел Rational. Посмотреть разницу.

3. Применить решения для вычисления числа пи.

4**. Задача на будущее: для дробей вида (n % m) типа
Rational написать функцию вывода десятичных знаков после запятой.
Особенно для гигантских n и m.

5. Написать рекурсивную формулу для вычисления чисел Фибоначчи.
Найти пороговое значение n для данной машины.

5*. Придумать решение для вычисления чисел Фибоначчи без ограничения на n.
